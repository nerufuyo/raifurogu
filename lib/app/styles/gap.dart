import 'package:flutter/material.dart';

class VerticalGap10 extends StatelessWidget {
  const VerticalGap10({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10);
  }
}

class HorizontalGap10 extends StatelessWidget {
  const HorizontalGap10({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10);
  }
}
